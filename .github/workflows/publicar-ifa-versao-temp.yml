# Publicação PWA — workflow temporário preservado apenas para execução manual
name: Publicar correção IFA e paleta

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  informar:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legado desativado para pushes automáticos."
